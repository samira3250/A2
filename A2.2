function multiplicationTable() {
  for (let i = 1; i <= 9; i++) {
    let row = ""; 
    for (let j = 1; j <= 9; j++) {
      let product = 0;
      for (let k = 0; k < j; k++) {
        product += i;
      }
      row += product + " ";
    }
    console.log(row);
  }
}
multiplicationTable();
